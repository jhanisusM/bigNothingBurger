<h1>hamburgers!</h1>

<h2>hamburgers that are not sleepy!</h2>

<ul>
  {{#each hamburgers}}
    {{#unless sleepy}}
      {{> hamburgers/hamburger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>hamburgers that are sleepy!</h2>

<ul>
  {{#each hamburgers}}
    {{#if sleepy}}
      {{> hamburgers/hamburger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a hamburger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">hamburger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add hamburger</button>
</form>
